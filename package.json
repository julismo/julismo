{
  "name": "julismo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "test": "npm run check && npm run test:unit && npm run test:e2e",
    "generate:qr": "node scripts/generate-qr.mjs",
    "generate:banners": "node scripts/generate-arm-banners.mjs",
    "generate:og": "node scripts/generate-og-image.mjs"
  },
  "dependencies": {
    "@calcom/embed-snippet": "^1.3.3",
    "astro": "^7.2.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@playwright/test": "^1.62.1",
    "@types/qrcode": "^1.5.6",
    "pngjs": "^5.0.0",
    "qrcode": "^1.5.4",
    "sharp": "^0.35.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
